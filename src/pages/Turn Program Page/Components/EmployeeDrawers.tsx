import React from "react";
// import { Button, Drawer, useDrawersStack } from "@mantine/core";
// import AppointmentHistory from "./AppointmentHistory";

// export const employeeDrawer = () => {
//   const stack = useDrawersStack(['employee-profile']);

//   return(
//     // <Drawer.Stack>
//       <Drawer {...stack.register('employee-profile')} opened={EmployeeOpened} onClose={closeEmployee} title={<h1>{currentEmployee?.name}</h1>}>
//       <AppointmentHistory appointments={currentEmployee?.appointments ?? []}/>
//       <Button 
//         onClick={() => currentEmployee?.clockedIn ? clockOut(currentEmployee.id): clockIn(currentEmployee?.id ?? '')}
//       >
//         {currentEmployee?.clockedIn ? "Clock Out": "Clock In"}
//       </Button>
//       </Drawer>

//     // </Drawer.Stack>
    
//   )
// }
